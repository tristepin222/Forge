module app.main

struct Point {
    x: i32
    y: i32
}

import app.main.{sum as total}

function sum(self: Point) -> i32 => self.x + self.y

function main() {
    value p: Point = Point { x: 2, y: 3 }
    print(p.total())
}
